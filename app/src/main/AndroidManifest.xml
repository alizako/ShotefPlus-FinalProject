<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="finals.shotefplus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/shotef_png"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/mainLabel"
            android:screenOrientation="portrait">

            <!--
                <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchResultsActivity" />
            -->
        </activity>
        <activity
            android:name=".activities.EntranceActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Search results activity -->
        <activity
            android:name=".activities.SearchResultsActivity"
            android:label="@string/searchResults">

            <!-- android:parentActivityName=".activities.MainActivity" -->


            <!--
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            -->
        </activity>
        <activity
            android:name=".activities.SignUpActivity"
            android:label="@string/signUpLabel" />
        <activity
            android:name=".activities.SearchActivity"
            android:label="@string/search" />
        <activity
            android:name=".activities.InsertReceiptActivity"
            android:label="@string/insertReceipt" />
        <activity
            android:name=".activities.ReceiptsActivity"
            android:label="@string/receipt" />
        <activity
            android:name=".activities.InsertPriceOfferActivity"
            android:label="@string/insertPriceOffer" />
        <activity
            android:name=".activities.PriceOffersActivity"
            android:label="@string/priceOffers" />
        <activity
            android:name=".activities.ExpensesActivity"
            android:label="@string/expenses" />
        <activity
            android:name=".activities.InsertExpenseActivity"
            android:label="@string/insertExpense" />
        <activity
            android:name=".activities.IncomesVsExpensesActivity"
            android:label="" />
        <activity
            android:name=".activities.ReportsActivity"
            android:label="" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/settings" />
        <activity
            android:name=".activities.CustomersActivity"
            android:label="@string/customers" />
        <activity
            android:name=".activities.InsertCustomerActivity"
            android:label="@string/insertCustomer" />
        <activity
            android:name=".activities.WorksActivity"
            android:label="@string/works" />
        <activity
            android:name=".activities.SignInActivity"
            android:label="@string/signIn" />

        <!-- Dialogs -->
        <activity
            android:name=".activities.UserProfileActivity"
            android:label="@string/userProfile"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.ChangeRegistrationInfoActivity"
            android:label="@string/changeCredential"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.MsgTemplateActivity"
            android:label="@string/msgTemplate"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.MailTemplateActivity"
            android:label="@string/mailTemplate"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.BusinessTypeActivity"
            android:label="@string/businessType"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.PriceOfferToWork"
            android:label="@string/PriceOfferToWork"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.ReceiptImageActivity"
            android:label="@string/ReceiptImage"
            android:theme="@style/NoActionBarDialog"/>
        <!-- Filters -->
        <activity
            android:name=".activities.FilterPriceOfferActivity"
            android:label="@string/filter"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.FilterWorkActivity"
            android:label="@string/filter"
            android:theme="@style/NoActionBarDialog" />
        <activity
            android:name=".activities.FilterReceiptActivity"
            android:label="@string/filter"
            android:theme="@style/NoActionBarDialog" />

    </application>

</manifest>